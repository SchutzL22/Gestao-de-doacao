/* =====================================================
   COPER FMP | js/config.js
   Configurações globais da aplicação.
   Altere apenas este arquivo para apontar para outro servidor.
   ===================================================== */

/**
 * URL base do backend — sem barra final.
 * Para trocar o ambiente (dev → produção), basta alterar esta linha.
 * Ex: 'https://api.coper-fmp.com.br'
 */
const API_BASE_URL = 'http://localhost:8080/api';
